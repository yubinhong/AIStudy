# AGENTS.md

> 本文件是“家庭 AI 学习助手”的仓库级持久指令。只放长期有效的行为、架构、安全和交付规则，不放单次需求。

## 1. 工作前必须读取

按以下顺序读取，并以更具体、更接近代码路径的指令为优先：

1. 本文件 `AGENTS.md`。
2. `AI_CONTEXT.md`：项目当前快照和主文档索引。
3. `PROJECT.md`：稳定目标、边界、设备分工、技术基线和环境现状。
4. `TASK.md`：当前活动任务；若状态不是 `READY` 或 `IN_PROGRESS`，不得擅自选择 `TODO.md` 中的任务。用户明确下达的当前请求不受此限制，但不得顺带执行其他 TODO。
5. 与当前任务有关的 `PRD.md`、`ARCHITECTURE.md`、`TESTING.md`、`SECURITY.md`。
6. 跨模块、高风险或长期任务继续读取 `PLANS.md`、`DECISIONS.md` 和相关 ADR。
7. 在 `ARCHITECTURE.md`、`PRD.md` 尚未完成项目化之前，涉及产品或架构边界的任务还必须查阅 `家庭AI学习助手_架构设计_v1.0.docx`。

如果文档与代码冲突：先指出冲突。默认以可运行代码、锁文件和测试反映的现状为事实，以已批准的 PRD、ADR 和设计基线为目标，不得把目标架构描述成已实现，也不得静默选择其一。

## 2. 当前仓库阶段

- 当前 P0 基础骨架已完成：目标 `apps/`、`services/`、`packages/`、`evals/`、`infra/compose/` 目录、最小入口、三类锁文件和 CI 草案已创建；合成 Profile/Device、Learning/Capture API 与两份数据库迁移已存在，部署仍不存在；Git 已初始化在 `master` 但无提交。
- ADR-0017 已接受以账号密码和可撤销会话替换 HMAC/PIN；TASK-0007 进一步删除 HMAC、Demo Header 和 Web 免登录旁路。API/Web/Flutter/Compose 运行时只允许用户名密码登录后的 Cookie/Bearer Session；Flutter 在登录前配置服务端地址，地址变更必须清理旧会话。真实 PostgreSQL、浏览器 E2E 和设备生命周期仍需验收。
- `PROJECT.md` 中的模块路径和命令在实际命令运行成功前不得声称对应能力已经存在；当前状态以 `TASK.md` 和 `TESTING.md` 的逐项验证记录为准。
- 初始化每个子项目时必须同时提交依赖清单、锁文件、最小测试、标准脚本/命令和忽略规则，并同步 `TESTING.md`、`AI_CONTEXT.md` 和本文件。
- 未经 ADR 或用户批准，不得用临时单文件实现替代目标模块边界，也不得为了快速演示跳过儿童数据、安全、契约或离线同步约束。

## 3. 任务执行协议

开始工作时：

- 用一句话复述目标、范围和完成标准。
- 检查工作区、相关入口、现有实现、测试、依赖清单和最近变更；若目录不是 Git 仓库，明确记录而不是假设工作区干净。
- 简单任务直接执行。出现以下任一条件时，先在 `PLANS.md` 建立或更新执行计划：
  - 同时影响 `apps/`、`services/`、`packages/`、`evals/`、`infra/` 中两个或更多顶层区域；
  - 涉及公开 API、OpenAPI/JSON Schema、数据库迁移、离线合并、安全/隐私边界或生产发布；
  - 预计超过半个工作日（约 4 小时）或需要多个可独立验收的里程碑。
- 缺少的信息只有在会显著改变产品行为、架构、安全、成本或破坏性操作时才询问；其他情况做最小假设，并用 `ASSUMPTION` 或 `TBD` 明确记录。

实施过程中：

- 只修改完成当前任务所需的文件，保留用户和其他开发者的无关改动。
- 优先沿用现有架构、命名、错误处理、日志和测试模式。
- 不进行顺手重构、依赖升级、全仓格式化或公共 API 变更，除非任务明确要求。
- 发现额外问题时记录到 `TODO.md`，不要扩大当前任务范围。
- 不绕过测试、类型检查、OpenAPI/Schema 校验、鉴权、儿童安全、审计或成本上限来让结果“看起来通过”。
- 新增外部依赖前说明用途、许可证、维护状态、替代方案、供应链风险和客户端体积/服务成本影响；核心依赖选择需要 ADR。

完成前：

- 按 `TESTING.md` 执行最小相关验证，再执行任务要求的完整质量门槛。
- 审查工作区差异，检查意外文件、调试代码、密钥、真实儿童数据、学习图片、生成物和不兼容变更。
- 更新任务要求涉及的文档、ADR、OpenAPI/JSON Schema、迁移说明和 `CHANGELOG.md`。
- 在 `TASK.md` 填写完成记录、验证结果、未执行项、剩余风险和回滚方式；若本轮是用户明确要求的文档初始化且未建立活动任务，在最终汇报中说明未更新 `TASK.md` 的原因。

## 4. 产品与架构不变量

### 产品原则

- 孩子端低干扰，一次只做一件事；AI 先提问和提示，不抢先给答案。
- 家长查看趋势、异常和建议，不建立儿童实时监控、公开排名或社交榜单。
- P1 聚焦小学数学单题闭环；语文、英语、语音和 Python 编程启蒙通过后续插件扩展，不污染核心任务/会话模型。
- 不复制或分发未经授权的教材、题库和教辅内容，不承诺专用学习机的护眼或硬件能力。

### 系统边界

- 后端从 FastAPI 模块化单体起步；只有独立扩容、隔离或团队边界有数据支持时才拆服务，并先写 ADR。
- `packages/contracts` 是 OpenAPI 和 AI JSON Schema 的唯一契约来源；客户端 SDK 由契约生成。不得在 Flutter、Web 和 API 中手工维护互相漂移的重复类型。
- PostgreSQL 是业务事实来源；Redis 只做缓存/队列；S3/MinIO 保存文件；pgvector 仅承载知识检索，不替代关系数据。
- 端侧 SQLite 只保存今日任务、会话和上传队列等离线数据。`Attempt`、`AuditEvent` 采用追加写；任务状态以服务端版本号合并，禁止用简单“最后写入覆盖”丢弃学习记录。
- 写接口必须支持 `idempotency-key`；文件上传使用短期签名 URL；重试需要有界、可观测并避免重复副作用。
- OCR、视觉、推理和低成本模型通过 Provider Adapter 接入。业务代码不得直接依赖某一云厂商 SDK 的响应形状。
- Capture 原图只进入家庭控制的存储与处理边界。本地 `PrivacySanitizer` 使用 OCR、规则和轻量视觉检测定位敏感区域并生成不可逆脱敏副本；OCR 不负责最终题目结构化。只有用户确认且安全门禁通过的脱敏副本可交给单一获批云端视觉 Provider 解析。
- 图片解析与 Tutor 是两个独立步骤：云端视觉结果必须先通过固定 Schema 和人工确认形成 `VerifiedQuestion`，才能进入 Tutor；不得在第一次看图时直接向孩子输出答案或把未确认结果作为学习事实。
- Tutor Policy 统一控制提示层级、结构化输出、敏感内容和成本上限。AI 输出通过固定 JSON Schema 校验后才能进入业务流程。

### 设备边界

- iPad mini 6：孩子主端，横屏学习、相机拍题和离线任务；Apple Pencil 可选，不得设为必需。
- Windows：P1 使用响应式 Web/PWA 承担重输入和家长管理；Flutter Desktop 与 Python 沙箱后置。
- iPhone 11、华为 nova 9：家长伴随端或临时拍题端，不作为长时孩子学习主端。
- 华为 Android 不依赖 Google Play Services；推送通过 HMS 适配器或应用内提醒抽象接入。

## 5. 安全、隐私与 AI 规则

- 家长和孩子均使用家庭内账号密码，不强制手机号，也不接入短信、邮箱、社交登录或 MFA；登录后使用可撤销会话。空数据库可创建一次性 `admin/admin123456` 引导账号，但只允许本机首次登录，改密前必须阻断家庭数据访问。所有读写必须按家庭、角色和孩子档案绑定授权，并包含反向越权、登录限速、会话撤销、CSRF 和首次改密测试。
- 儿童资料、学习记录和图片遵循最小采集、最短保留和用途限定。图片默认短期保存；必须支持家庭数据导出和删除。
- 真实密钥、令牌、个人数据、儿童图片和生产数据库内容不得进入代码、提交、日志、截图、Prompt、评测集或测试夹具。
- 外部输入在信任边界验证：包括图片类型/大小、OCR 文本、URL、OpenAPI 参数、AI JSON、导入内容和设备同步事件。
- 图片外发采用“App 单题裁剪 → 服务端清除元数据并自动脱敏 → 用户预览/手动涂抹并确认”三层门禁。自动脱敏不得宣传为绝对匿名；低置信度、大面积真实人脸、身份证信息或无法安全裁剪时必须阻断外发。
- 原图、MinIO URL、对象键和敏感 OCR 文本不得发送给云 Provider。单次图片解析只允许一个获批 Provider；不得因失败自动把同一图片广播给多个 Provider。
- AI 调用必须记录稳定事件名及结构化字段：模型/Provider、模型版本、Prompt/Policy 版本、输入输出摘要或不可逆指纹、Schema 版本、延迟、token/成本、置信度和结果状态；不得记录原始敏感内容。
- 低置信度识别或推理先要求用户校正。未经验证的模型回答不得直接作为标准答案、知识点结论或掌握度事实入库。
- 模型、Prompt、Tutor Policy 或路由变化必须运行固定 AI 评测集，比较质量、安全、延迟和成本，并保留可回滚版本。
- 数据保留周期、适用法域和正式儿童隐私条款尚为 `TBD`；任何生产部署前必须由项目 Owner 明确并同步 `SECURITY.md` 与 ADR。

## 6. 技术与代码规范

### 技术基线

| 区域 | 语言/框架 | 版本策略 | 包管理器 |
| --- | --- | --- | --- |
| `apps/child_flutter` | Dart / Flutter（iOS、Android） | P0 选择 Flutter stable 并用仓库配置和锁文件固定；具体版本当前 `TBD` | `flutter pub` |
| `apps/web` | TypeScript / Next.js | P0 选择受支持的 Node LTS 和 Next.js 版本并用 `.nvmrc`/`package.json`/锁文件固定；具体版本当前 `TBD` | `pnpm` |
| `services/api` | Python / FastAPI / Worker | Python `3.12`；FastAPI 和 Worker 版本由 `pyproject.toml`/锁文件固定 | `uv` |
| 数据/本地设施 | PostgreSQL、pgvector、Redis、S3/MinIO、Docker Compose | P0 在 Compose 和迁移配置中固定，具体版本当前 `TBD` | Docker Compose / 各官方镜像 |

包管理器是 P0 目标基线；若初始化代码采用不同工具，必须在合并前说明原因、更新本文件与 `PROJECT.md`/`TESTING.md`，核心工具变化需 ADR。必须提交 `pnpm-lock.yaml`、`uv.lock`、`pubspec.lock` 等适用锁文件；禁止同时维护多个解决同一子项目依赖的锁文件。

### 标准命令状态

`TESTING.md` 是命令、测试矩阵和质量门槛的唯一事实来源。当前 API、Web、Compose 配置以及 Flutter 格式/分析/单元测试已有实际验证；Flutter Android/iOS 原生构建已通过本地验证，E2E/AI/契约生成/安全扫描仍待后续实现。

P0 创建模块时必须实现并验证 `TESTING.md` 中的目标入口；如实际脚本名称不同，先运行确认，再同步 `TESTING.md`。本文件只保留行为规则，不复制命令表，避免漂移。禁止把未运行、路径不存在或仅作为目标的命令报告为通过。

代码要求：

- 公共接口、OpenAPI、JSON Schema 或事件变更必须说明兼容性、迁移和回滚策略；优先做向后兼容的增量变化。
- 新增行为必须有测试；修复缺陷先添加能复现失败的回归测试。
- Python 公共函数和模块边界使用类型标注；TypeScript 不以 `any` 绕过契约；Dart 保持空安全。
- 错误信息应可操作但不得泄露敏感信息；日志使用结构化字段和稳定事件名，不记录原始图片、题目全文、令牌或儿童身份信息。
- 配置通过环境或配置文件注入；提交脱敏的 `.env.example`，禁止硬编码环境特定地址和凭据。
- 数据库变更使用版本化迁移，必须可向前部署，并说明回滚或前滚修复、旧数据兼容、并发和索引影响。

## 7. 测试与发布门槛

- 修改纯函数：运行对应单元测试、格式、Lint 和类型检查。
- 修改 OpenAPI/Schema：运行契约生成/差异检查、API 测试和所有受影响客户端检查。
- 修改数据库或离线同步：验证迁移、旧数据、重复请求、并发、断网重连、幂等和回滚/前滚。
- 修改 UI：验证目标设备职责、横竖屏、弱网、相机/存储权限、键盘、空/错状态和可访问性。
- 修改图片脱敏/AI：运行固定 eval，覆盖敏感信息漏检/误遮挡、元数据与原始像素清除、用户确认绑定、单 Provider 外发、正确提示层级、拒绝直接代答、低置信度校正、Schema 失败、敏感内容和成本上限。
- P1 发布前必须满足：核心 E2E 全通过；四类设备完成弱网/横竖屏/权限回归；契约测试、迁移回滚、AI 评测、成本告警、备份恢复和儿童数据删除均有记录。
- Flaky test 不得通过无解释重跑掩盖；必须记录根因、隔离范围和恢复条件。

## 8. 操作授权边界

- “解释、分析、审查、诊断”默认只读，不实现修复，除非用户明确要求修改。
- “实现、修复、构建、更新”授权进行任务范围内的代码和文档修改及必要验证。
- 删除数据、重写历史、强制推送、生产部署、外部消息、创建工单、修改云资源、导入真实儿童数据等操作必须获得明确授权。
- 本地启动 Docker 服务若会覆盖持久卷或连接非本地资源，先确认配置；不得默认连接 staging/production。
- 默认不提交、不推送、不创建 PR，除非用户明确要求。

## 9. Git 与变更纪律

- 不使用 `git reset --hard`、强制推送或覆盖式 checkout 清理未知改动。
- 提交应小而完整，消息采用 Conventional Commits 风格：`type(scope): imperative summary`。
- 允许的常用 `type`：`feat`、`fix`、`test`、`docs`、`refactor`、`perf`、`build`、`ci`、`chore`、`security`。
- `scope` 使用稳定模块名，例如 `child`、`web`、`api`、`contracts`、`evals`、`infra`、`docs`；重大不兼容变化使用 `!` 并提供迁移说明。
- 每个提交必须能说明：为什么改、改了什么、如何验证。不得提交密钥、真实数据、学习图片、缓存、IDE 文件或可重建产物。

## 10. 文档同步矩阵

| 变更类型 | 必须检查/更新 |
| --- | --- |
| 用户行为、设备职责或需求 | `PRD.md`、`PROJECT.md`、`TASK.md`、`CHANGELOG.md` |
| 系统边界、组件、契约或核心依赖 | `ARCHITECTURE.md`、`DECISIONS.md`、相关 ADR、`PROJECT.md` |
| 命令、工具链或质量门槛 | `AGENTS.md`、`TESTING.md`、CI 配置 |
| 安全边界、儿童数据或 AI 数据处理 | `SECURITY.md`、相关 ADR、`PROJECT.md` |
| 部署、告警、成本上限、备份或回滚 | `RUNBOOK.md`、`SECURITY.md` |
| 当前状态或活动任务变化 | `AI_CONTEXT.md`、`TASK.md`、`TODO.md` |
| 已交付且用户/运维可感知的变化 | `CHANGELOG.md` |

稳定事实只保留一个主文档；其他文档用摘要和链接引用，避免复制后漂移。若设计稿与后续获批 ADR 冲突，以新 ADR 为目标，并在设计稿替代前记录差异。

## 11. Definition of Done

只有同时满足以下条件才可声明完成：

- 验收标准逐项满足并有测试、日志、截图或人工验收证据。
- 相关格式、Lint、类型、测试、契约、AI eval 和构建检查按变更风险通过；无法运行的项目明确说明原因、替代验证和残余风险。
- 儿童数据、权限、离线幂等、AI 可控性、成本、API/数据兼容和回滚已按影响面检查，无未解释的高风险。
- 工作区无意外文件、调试代码、密钥、真实数据、学习图片或不应提交的生成物。
- 文档、ADR、契约、迁移和变更记录按同步矩阵更新。
- `TASK.md` 有完成记录；若本轮没有活动任务，则最终汇报明确记录该例外。
- 最终汇报包含结果、关键文件、验证命令与结果、未执行项和剩余风险；不要只描述过程。
