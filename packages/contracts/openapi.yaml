openapi: 3.1.0
info:
  title: 家庭 AI 学习助手 API
  version: 0.6.0
  description: Self-hosted Household account/session and learning contract vertical slices.
servers:
  - url: http://localhost:8000
paths:
  /healthz:
    get:
      operationId: getHealth
      summary: Service health check
      responses:
        '200':
          description: Service is ready to receive requests.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthResponse'
  /auth/login:
    post:
      operationId: login
      summary: Create a revocable account session
      description: The Web client receives an HttpOnly study_session cookie. Flutter requests client=flutter and stores the returned opaque token in platform secure storage.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LoginRequest'
      responses:
        '200':
          description: Session created.
          headers:
            Set-Cookie:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LoginResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /auth/me:
    get:
      operationId: getCurrentAccount
      summary: Read the current account
      security:
        - SessionCookie: []
        - BearerSession: []
      responses:
        '200':
          description: Current account.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccountView'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /auth/change-password:
    post:
      operationId: changePassword
      summary: Change the current password and rotate all sessions
      security:
        - SessionCookie: []
        - BearerSession: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ChangePasswordRequest'
      responses:
        '200':
          description: Password changed and a new session issued.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LoginResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /auth/logout:
    post:
      operationId: logout
      summary: Revoke the current session
      security:
        - SessionCookie: []
        - BearerSession: []
      responses:
        '204':
          description: Session revoked.
        '401':
          $ref: '#/components/responses/Unauthorized'
  /auth/households/{household_id}/accounts:
    parameters:
      - $ref: '#/components/parameters/HouseholdId'
    get:
      operationId: listAccounts
      summary: List accounts in one household
      security:
        - SessionCookie: []
        - BearerSession: []
      responses:
        '200':
          description: Accounts without password hashes.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/AccountView'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
  /auth/households/{household_id}/accounts/children:
    parameters:
      - $ref: '#/components/parameters/HouseholdId'
    post:
      operationId: createChildAccount
      summary: Create a child account bound to one ChildProfile
      security:
        - SessionCookie: []
        - BearerSession: []
      parameters:
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateChildAccountRequest'
      responses:
        '201':
          description: Child account created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccountView'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/IdempotencyConflict'
  /auth/households/{household_id}/accounts/{account_id}/status:
    parameters:
      - $ref: '#/components/parameters/HouseholdId'
      - name: account_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    patch:
      operationId: setAccountStatus
      summary: Enable or disable a child account
      security:
        - SessionCookie: []
        - BearerSession: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SetAccountStatusRequest'
      responses:
        '200':
          description: Updated child account.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccountView'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
  /auth/households/{household_id}/accounts/{account_id}/reset-password:
    parameters:
      - $ref: '#/components/parameters/HouseholdId'
      - name: account_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      operationId: resetAccountPassword
      summary: Reset a child account password and revoke its sessions
      security:
        - SessionCookie: []
        - BearerSession: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ResetPasswordRequest'
      responses:
        '200':
          description: Password reset; no password is returned.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccountView'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
  /households/{household_id}/children:
    parameters:
      - $ref: '#/components/parameters/HouseholdId'
    get:
      operationId: listChildren
      summary: List children in the caller's household
      security:
        - SessionCookie: []
        - BearerSession: []
      responses:
        '200':
          description: Children visible to the caller.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/ChildProfile'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    post:
      operationId: createChild
      summary: Create a child profile in the caller's household
      security:
        - SessionCookie: []
        - BearerSession: []
      parameters:
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateChildRequest'
      responses:
        '201':
          description: Child profile created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ChildProfile'
        '200':
          description: Idempotent replay of an earlier create request.
          headers:
            Idempotency-Replayed:
              schema:
                type: string
                const: 'true'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ChildProfile'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/InvalidRequest'
        '409':
          $ref: '#/components/responses/IdempotencyConflict'
  /households/{household_id}/children/{child_id}:
    parameters:
      - $ref: '#/components/parameters/HouseholdId'
      - name: child_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: getChild
      summary: Get a child profile in the caller's household
      security:
        - SessionCookie: []
        - BearerSession: []
      responses:
        '200':
          description: Child profile.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ChildProfile'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    delete:
      operationId: deleteChild
      summary: Delete a child profile after cascading Capture object deletion
      description: Local/CI synthetic deletion only. The profile is not removed when any Capture object deletion fails; the same idempotency key may be retried.
      security:
        - SessionCookie: []
        - BearerSession: []
      parameters:
        - $ref: '#/components/parameters/IdempotencyKey'
      responses:
        '204':
          description: Child profile and all claimed Capture objects were deleted.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/InvalidRequest'
        '503':
          description: Deletion is incomplete or temporarily unavailable; retry is safe.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /households/{household_id}/devices:
    parameters:
      - $ref: '#/components/parameters/HouseholdId'
    get:
      operationId: listDevices
      summary: List devices in the caller's household
      security:
        - SessionCookie: []
        - BearerSession: []
      responses:
        '200':
          description: Devices visible to the caller.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Device'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
    post:
      operationId: registerDevice
      summary: Register a device in the caller's household
      security:
        - SessionCookie: []
        - BearerSession: []
      parameters:
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateDeviceRequest'
      responses:
        '201':
          description: Device registered.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Device'
        '200':
          description: Idempotent replay of an earlier registration.
          headers:
            Idempotency-Replayed:
              schema:
                type: string
                const: 'true'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Device'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/InvalidRequest'
        '409':
          $ref: '#/components/responses/IdempotencyConflict'
  /households/{household_id}/tasks:
    parameters:
      - $ref: '#/components/parameters/HouseholdId'
    get:
      operationId: listTasks
      summary: List tasks visible to the caller
      security:
        - SessionCookie: []
        - BearerSession: []
      responses:
        '200':
          description: Tasks visible to the parent or bound child.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/StudyTask'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
    post:
      operationId: createTask
      summary: Create a math task for a child in the caller's household
      security:
        - SessionCookie: []
        - BearerSession: []
      parameters:
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateTaskRequest'
      responses:
        '201':
          description: Task created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StudyTask'
        '200':
          description: Idempotent task-create replay.
          headers:
            Idempotency-Replayed:
              schema:
                type: string
                const: 'true'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StudyTask'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/IdempotencyConflict'
  /households/{household_id}/tasks/{task_id}/sessions:
    parameters:
      - $ref: '#/components/parameters/HouseholdId'
      - $ref: '#/components/parameters/TaskId'
    post:
      operationId: startStudySession
      summary: Start a session for a task assigned to the bound child
      security:
        - SessionCookie: []
        - BearerSession: []
      parameters:
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/StartStudySessionRequest'
      responses:
        '201':
          description: Study session started.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StudySession'
        '200':
          description: Idempotent start-session replay.
          headers:
            Idempotency-Replayed:
              schema:
                type: string
                const: 'true'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StudySession'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/IdempotencyConflict'
  /households/{household_id}/sessions/{session_id}/attempts:
    parameters:
      - $ref: '#/components/parameters/HouseholdId'
      - $ref: '#/components/parameters/SessionId'
    post:
      operationId: recordAttempt
      summary: Append an attempt to the bound child's active study session
      security:
        - SessionCookie: []
        - BearerSession: []
      parameters:
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RecordAttemptRequest'
      responses:
        '201':
          description: Attempt appended.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Attempt'
        '200':
          description: Idempotent attempt replay.
          headers:
            Idempotency-Replayed:
              schema:
                type: string
                const: 'true'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Attempt'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/IdempotencyConflict'
  /households/{household_id}/sync-batches:
    parameters:
      - $ref: '#/components/parameters/HouseholdId'
    post:
      operationId: syncOfflineBatch
      summary: Apply a versioned batch of offline attempt events atomically
      security:
        - SessionCookie: []
        - BearerSession: []
      parameters:
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SyncBatchRequest'
      responses:
        '200':
          description: Batch applied or replayed without partial writes.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SyncBatchResult'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/IdempotencyConflict'
  /households/{household_id}/sessions/{session_id}/captures:
    parameters:
      - $ref: '#/components/parameters/HouseholdId'
      - $ref: '#/components/parameters/SessionId'
    get:
      operationId: listCaptures
      summary: List Capture metadata visible to the bound child in a study session
      security:
        - SessionCookie: []
        - BearerSession: []
      responses:
        '200':
          description: Capture metadata. This endpoint never returns media bytes or object URLs.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Capture'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
    post:
      operationId: createCapture
      summary: Register bounded image metadata and require manual correction
      description: No image bytes, signed URLs, object keys, or OCR Provider calls are accepted in this contract version.
      security:
        - SessionCookie: []
        - BearerSession: []
      parameters:
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateCaptureRequest'
      responses:
        '201':
          description: Capture metadata registered with needs_correction status.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Capture'
        '200':
          description: Idempotent Capture-create replay.
          headers:
            Idempotency-Replayed:
              schema:
                type: string
                const: 'true'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Capture'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/IdempotencyConflict'
  /households/{household_id}/captures/{capture_id}/corrections:
    parameters:
      - $ref: '#/components/parameters/HouseholdId'
      - name: capture_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      operationId: correctCapture
      summary: Append a manual correction for a Capture owned by the bound child
      security:
        - SessionCookie: []
        - BearerSession: []
      parameters:
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CorrectCaptureRequest'
      responses:
        '201':
          description: Manual correction appended.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CaptureCorrection'
        '200':
          description: Idempotent correction replay.
          headers:
            Idempotency-Replayed:
              schema:
                type: string
                const: 'true'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CaptureCorrection'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: Capture version or idempotency conflict.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /households/{household_id}/captures/{capture_id}/save:
    parameters:
      - $ref: '#/components/parameters/HouseholdId'
      - name: capture_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      operationId: saveCapture
      summary: Save a Capture image beyond its default retention window
      description: Parent-only local/CI operation. Saving does not expose media bytes, object keys, or storage credentials.
      security:
        - SessionCookie: []
        - BearerSession: []
      parameters:
        - $ref: '#/components/parameters/IdempotencyKey'
      responses:
        '204':
          description: Capture image is marked parent-saved.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/IdempotencyConflict'
  /households/{household_id}/captures/{capture_id}/media:
    parameters:
      - $ref: '#/components/parameters/HouseholdId'
      - name: capture_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    delete:
      operationId: deleteCaptureMedia
      summary: Immediately delete one Capture image
      description: Parent-only local/CI operation. Deletion is private-object scoped, retryable, and does not delete Capture metadata.
      security:
        - SessionCookie: []
        - BearerSession: []
      parameters:
        - $ref: '#/components/parameters/IdempotencyKey'
      responses:
        '204':
          description: Capture image deletion completed or was replayed.
          headers:
            Idempotency-Replayed:
              schema:
                type: string
                const: 'true'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/IdempotencyConflict'
        '503':
          description: Object storage deletion is temporarily unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /households/{household_id}/sessions/{session_id}/capture-uploads:
    parameters:
      - $ref: '#/components/parameters/HouseholdId'
      - $ref: '#/components/parameters/SessionId'
    post:
      operationId: beginCaptureUpload
      summary: Create a pending Capture and issue one short-lived private PUT URL
      description: The response has no separate object-key field. The signed URL is short-lived and must not be logged or persisted by clients.
      security:
        - SessionCookie: []
        - BearerSession: []
      parameters:
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateCaptureRequest'
      responses:
        '201':
          description: Pending Capture created and a short-lived upload URL issued.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CaptureUpload'
        '200':
          description: Idempotent replay while the Capture upload remains pending. A replacement short-lived URL may be issued.
          headers:
            Idempotency-Replayed:
              schema:
                type: string
                const: 'true'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CaptureUpload'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/IdempotencyConflict'
        '503':
          description: Private object storage is temporarily unavailable; retry with the same idempotency key.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /households/{household_id}/captures/{capture_id}/upload-confirmations:
    parameters:
      - $ref: '#/components/parameters/HouseholdId'
      - name: capture_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      operationId: confirmCaptureUpload
      summary: Confirm a server-verified private object before manual correction
      description: The API verifies object media type and byte size server-side. It never accepts an object key or bytes from the client.
      security:
        - SessionCookie: []
        - BearerSession: []
      parameters:
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ConfirmCaptureUploadRequest'
      responses:
        '201':
          description: Private object verified; Capture advances to needs_correction.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Capture'
        '200':
          description: Idempotent upload-confirmation replay.
          headers:
            Idempotency-Replayed:
              schema:
                type: string
                const: 'true'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Capture'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: Object verification, Capture state/version, or idempotency conflict.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /households/{household_id}/captures/{capture_id}/ocr-jobs:
    parameters:
      - $ref: '#/components/parameters/HouseholdId'
      - name: capture_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      operationId: enqueueOcrJob
      summary: Enqueue local OCR for a server-confirmed Capture
      description: The endpoint only records a bounded job request. OCR runs in a separate local Worker and returns manually confirmable candidates; it never accepts image bytes, object keys, or external Provider selection.
      security:
        - SessionCookie: []
        - BearerSession: []
      parameters:
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EnqueueOcrJobRequest'
      responses:
        '202':
          description: OCR job accepted by the local/CI queue.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OcrJobReceipt'
        '200':
          description: Idempotent OCR job enqueue replay.
          headers:
            Idempotency-Replayed:
              schema:
                type: string
                const: 'true'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OcrJobReceipt'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: Capture is not ready for OCR or the idempotency key conflicts.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /households/{household_id}/captures/{capture_id}/ocr-jobs/{job_id}:
    parameters:
      - $ref: '#/components/parameters/HouseholdId'
      - name: capture_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: job_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: getOcrJob
      summary: Read bounded local OCR job status
      description: The response contains stable lifecycle fields only; Provider errors, image bytes, object keys, and OCR text are not returned by this endpoint.
      security:
        - SessionCookie: []
        - BearerSession: []
      responses:
        '200':
          description: OCR job status.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OcrJobReceipt'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
  /households/{household_id}/captures/{capture_id}/image-analysis-jobs:
    parameters:
      - $ref: '#/components/parameters/HouseholdId'
      - name: capture_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      operationId: startImageAnalysis
      summary: Record a user-confirmed sanitization receipt for one Capture
      description: The service queues a job only when NewAPI is explicitly enabled and the user-confirmed sanitization hash matches the Capture; otherwise it records a blocked job. Only the confirmed derivative may reach the self-hosted Provider.
      security:
        - SessionCookie: []
        - BearerSession: []
      parameters:
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/StartImageAnalysisRequest'
      responses:
        '202':
          description: Receipt accepted; status is queued when the self-hosted Provider is enabled, otherwise blocked.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ImageAnalysisJobReceipt'
        '200':
          description: Idempotent replay of the same receipt.
          headers:
            Idempotency-Replayed:
              schema:
                type: string
                const: 'true'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ImageAnalysisJobReceipt'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: Capture version, upload state, or idempotency conflict.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /households/{household_id}/captures/{capture_id}/image-analysis-jobs/{job_id}:
    parameters:
      - $ref: '#/components/parameters/HouseholdId'
      - name: capture_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: job_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: getImageAnalysisJob
      summary: Read provider-neutral ImageAnalysis job state
      description: The response contains only lifecycle, receipt hash, and stable error fields; no image, URL, object key, OCR text, or Provider response is returned.
      security:
        - SessionCookie: []
        - BearerSession: []
      responses:
        '200':
          description: ImageAnalysis job state.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ImageAnalysisJobReceipt'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
  /households/{household_id}/captures/{capture_id}/image-analysis-jobs/{job_id}/extraction:
    parameters:
      - $ref: '#/components/parameters/HouseholdId'
      - name: capture_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: job_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: getQuestionExtraction
      summary: Read an unverified question extraction for manual review
      description: Returns only schema-validated extraction fields. The result is not a verified question and must not be used as a Tutor fact before human confirmation.
      security:
        - SessionCookie: []
        - BearerSession: []
      responses:
        '200':
          description: Unverified question extraction.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/QuestionExtractionRecord'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: Extraction has not completed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /households/{household_id}/captures/{capture_id}/image-analysis-jobs/{job_id}/extraction/verify:
    parameters:
      - $ref: '#/components/parameters/HouseholdId'
      - name: capture_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: job_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      operationId: verifyQuestionExtraction
      summary: Confirm and edit one extraction into a VerifiedQuestion
      description: The unverified extraction remains immutable. Only explicitly submitted fields become a Tutor-safe fact after Household and Capture version checks.
      security:
        - SessionCookie: []
        - BearerSession: []
      parameters:
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VerifyQuestionRequest'
      responses:
        '201':
          description: Human-confirmed question fact created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VerifiedQuestion'
        '200':
          description: Idempotent replay of the same confirmation.
          headers:
            Idempotency-Replayed:
              schema:
                type: string
                const: 'true'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VerifiedQuestion'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: Capture version, extraction readiness, or idempotency conflict.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /households/{household_id}/captures/{capture_id}/image-analysis-jobs/{job_id}/verified-question:
    parameters:
      - $ref: '#/components/parameters/HouseholdId'
      - name: capture_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: job_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: getVerifiedQuestion
      summary: Read a human-confirmed question fact
      security:
        - SessionCookie: []
        - BearerSession: []
      responses:
        '200':
          description: Verified question.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VerifiedQuestion'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
  /households/{household_id}/tutor/hints:
    parameters:
      - $ref: '#/components/parameters/HouseholdId'
    post:
      operationId: createTutorHint
      summary: Return a provider-free graduated hint for a verified question
      description: The current implementation is a deterministic zero-cost local fallback. It requires a child-bound corrected Capture and never returns a direct answer or calls a Provider.
      security:
        - SessionCookie: []
        - BearerSession: []
      parameters:
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/StartTutorHintRequest'
      responses:
        '200':
          description: Bounded offline hint; direct_answer is always null.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TutorHintResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: Capture has not been manually confirmed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /households/{household_id}/captures/{capture_id}/ocr-results/{result_id}:
    parameters:
      - $ref: '#/components/parameters/HouseholdId'
      - name: capture_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: result_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: getOcrResult
      summary: Get unverified OCR candidates for the bound child
      description: The child-only response is rechecked against Household, Child, and Capture ownership. Candidates remain unverified and must be manually confirmed before entering the learning workflow.
      security:
        - SessionCookie: []
        - BearerSession: []
      responses:
        '200':
          description: OCR result and candidates awaiting manual confirmation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OcrResultWithCandidates'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
    post:
      operationId: confirmOcrCandidate
      summary: Confirm one OCR candidate as a manual Capture correction
      description: The server resolves the candidate by ID under the bound Household, Child, result, and Capture, then appends its text to CaptureCorrection. The OCR result remains an unverified model output.
      security:
        - SessionCookie: []
        - BearerSession: []
      parameters:
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ConfirmOcrCandidateRequest'
      responses:
        '201':
          description: Candidate text appended as a manual Capture correction.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CaptureCorrection'
        '200':
          description: Idempotent confirmation replay.
          headers:
            Idempotency-Replayed:
              schema:
                type: string
                const: 'true'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CaptureCorrection'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: Capture version, state, or idempotency conflict.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
components:
  parameters:
    HouseholdId:
      name: household_id
      in: path
      required: true
      schema:
        type: string
        format: uuid
    IdempotencyKey:
      name: Idempotency-Key
      in: header
      required: true
      schema:
        type: string
        minLength: 8
        maxLength: 128
    TaskId:
      name: task_id
      in: path
      required: true
      schema:
        type: string
        format: uuid
    SessionId:
      name: session_id
      in: path
      required: true
      schema:
        type: string
        format: uuid
  securitySchemes:
    BearerSession:
      type: http
      scheme: bearer
      bearerFormat: opaque 256-bit session token
      description: Flutter session token; only a SHA-256 digest is stored by the API.
    SessionCookie:
      type: apiKey
      in: cookie
      name: study_session
      description: HttpOnly SameSite=Lax Web session cookie.
  schemas:
    LoginRequest:
      type: object
      additionalProperties: false
      required: [username, password]
      properties:
        username:
          type: string
          minLength: 3
          maxLength: 80
        password:
          type: string
          minLength: 1
          maxLength: 128
        client:
          type: string
          enum: [web, flutter]
          default: web
    LoginResponse:
      type: object
      additionalProperties: false
      required: [account, expires_at]
      properties:
        account:
          $ref: '#/components/schemas/AccountView'
        access_token:
          type: string
          nullable: true
        expires_at:
          type: string
          format: date-time
    AccountView:
      type: object
      additionalProperties: false
      required: [id, household_id, username, role, must_change_password, status, created_at]
      properties:
        id:
          type: string
          format: uuid
        household_id:
          type: string
          format: uuid
        username:
          type: string
        role:
          type: string
          enum: [parent, child]
        child_id:
          type: string
          format: uuid
          nullable: true
        must_change_password:
          type: boolean
        status:
          type: string
          enum: [active, disabled]
        created_at:
          type: string
          format: date-time
    ChangePasswordRequest:
      type: object
      additionalProperties: false
      required: [current_password, new_password]
      properties:
        current_password:
          type: string
          minLength: 1
          maxLength: 128
        new_password:
          type: string
          minLength: 1
          maxLength: 128
    CreateChildAccountRequest:
      type: object
      additionalProperties: false
      required: [username, password, child_id]
      properties:
        username:
          type: string
          minLength: 3
          maxLength: 80
        password:
          type: string
          minLength: 1
          maxLength: 128
        child_id:
          type: string
          format: uuid
    SetAccountStatusRequest:
      type: object
      additionalProperties: false
      required: [enabled, current_password]
      properties:
        enabled:
          type: boolean
        current_password:
          type: string
          minLength: 1
          maxLength: 128
    ResetPasswordRequest:
      type: object
      additionalProperties: false
      required: [current_password, new_password]
      properties:
        current_password:
          type: string
          minLength: 1
          maxLength: 128
        new_password:
          type: string
          minLength: 1
          maxLength: 128
    HealthResponse:
      type: object
      additionalProperties: false
      required: [status, service, version]
      properties:
        status:
          type: string
          const: ok
        service:
          type: string
          const: study-api
        version:
          type: string
          const: 0.4.0
    CreateChildRequest:
      type: object
      additionalProperties: false
      required: [display_name, grade, curriculum_version, subjects]
      properties:
        display_name:
          type: string
          minLength: 1
          maxLength: 80
        grade:
          type: integer
          minimum: 1
          maximum: 6
        curriculum_version:
          type: string
          minLength: 1
          maxLength: 80
        subjects:
          type: array
          minItems: 1
          items:
            type: string
            enum: [math]
    ChildProfile:
      type: object
      additionalProperties: false
      required: [id, household_id, display_name, grade, curriculum_version, subjects, created_at]
      properties:
        id:
          type: string
          format: uuid
        household_id:
          type: string
          format: uuid
        display_name:
          type: string
        grade:
          type: integer
        curriculum_version:
          type: string
        subjects:
          type: array
          items:
            type: string
            enum: [math]
        created_at:
          type: string
          format: date-time
    CreateDeviceRequest:
      type: object
      additionalProperties: false
      required: [kind, platform, display_name]
      properties:
        kind:
          type: string
          enum: [child, parent]
        platform:
          type: string
          enum: [ios, android, web]
        display_name:
          type: string
          minLength: 1
          maxLength: 80
    Device:
      type: object
      additionalProperties: false
      required: [id, household_id, kind, platform, display_name, status, registered_at]
      properties:
        id:
          type: string
          format: uuid
        household_id:
          type: string
          format: uuid
        kind:
          type: string
          enum: [child, parent]
        platform:
          type: string
          enum: [ios, android, web]
        display_name:
          type: string
        status:
          type: string
          const: active
        registered_at:
          type: string
          format: date-time
    CreateTaskRequest:
      type: object
      additionalProperties: false
      required: [child_id, title, subject, scheduled_for]
      properties:
        child_id:
          type: string
          format: uuid
        title:
          type: string
          minLength: 1
          maxLength: 120
        subject:
          type: string
          enum: [math]
        scheduled_for:
          type: string
          format: date
    StudyTask:
      type: object
      additionalProperties: false
      required: [id, household_id, child_id, title, subject, scheduled_for, status, version, created_at]
      properties:
        id:
          type: string
          format: uuid
        household_id:
          type: string
          format: uuid
        child_id:
          type: string
          format: uuid
        title:
          type: string
        subject:
          type: string
          enum: [math]
        scheduled_for:
          type: string
          format: date
        status:
          type: string
          enum: [assigned, in_progress]
        version:
          type: integer
          minimum: 1
        created_at:
          type: string
          format: date-time
    StartStudySessionRequest:
      type: object
      additionalProperties: false
      required: [expected_task_version]
      properties:
        expected_task_version:
          type: integer
          minimum: 1
    StudySession:
      type: object
      additionalProperties: false
      required: [id, household_id, child_id, task_id, task_version, status, started_at]
      properties:
        id:
          type: string
          format: uuid
        household_id:
          type: string
          format: uuid
        child_id:
          type: string
          format: uuid
        task_id:
          type: string
          format: uuid
        task_version:
          type: integer
          minimum: 1
        status:
          type: string
          const: active
        started_at:
          type: string
          format: date-time
    CreateCaptureRequest:
      type: object
      additionalProperties: false
      required: [media_type, byte_size, content_sha256]
      properties:
        media_type:
          type: string
          enum: [image/jpeg, image/png]
        byte_size:
          type: integer
          minimum: 1
          maximum: 8000000
        content_sha256:
          type: string
          pattern: '^[a-f0-9]{64}$'
    Capture:
      type: object
      additionalProperties: false
      required: [id, household_id, child_id, session_id, media_type, byte_size, content_sha256, status, version, created_at]
      properties:
        id:
          type: string
          format: uuid
        household_id:
          type: string
          format: uuid
        child_id:
          type: string
          format: uuid
        session_id:
          type: string
          format: uuid
        media_type:
          type: string
          enum: [image/jpeg, image/png]
        byte_size:
          type: integer
        content_sha256:
          type: string
          pattern: '^[a-f0-9]{64}$'
        status:
          type: string
          enum: [upload_pending, needs_correction, corrected]
        version:
          type: integer
          minimum: 1
        created_at:
          type: string
          format: date-time
    CaptureUpload:
      type: object
      additionalProperties: false
      required: [capture, upload_url, upload_expires_at]
      properties:
        capture:
          $ref: '#/components/schemas/Capture'
        upload_url:
          type: string
          format: uri
          description: Short-lived PUT URL; clients must not persist or log it.
        upload_expires_at:
          type: string
          format: date-time
    ConfirmCaptureUploadRequest:
      type: object
      additionalProperties: false
      required: [expected_capture_version]
      properties:
        expected_capture_version:
          type: integer
          minimum: 1
    PrivacySanitization:
      type: object
      additionalProperties: false
      required: [schema_version, sanitizer_version, safe_to_upload, requires_confirmation, sensitive_types, region_count, face_detected, qr_detected, barcode_detected, blocked_reasons, sanitized_derivative_sha256]
      properties:
        schema_version:
          type: string
          const: privacy-sanitization.v1
        sanitizer_version:
          type: string
          minLength: 1
          maxLength: 64
        safe_to_upload:
          type: boolean
        requires_confirmation:
          type: boolean
          const: true
        sensitive_types:
          type: array
          uniqueItems: true
          maxItems: 32
          items:
            type: string
            enum: [name, school, class, grade, student_id, exam_id, seat_number, phone, address, signature, face, qr_code, barcode]
        region_count:
          type: integer
          minimum: 0
          maximum: 256
        face_detected:
          type: boolean
        qr_detected:
          type: boolean
        barcode_detected:
          type: boolean
        blocked_reasons:
          type: array
          maxItems: 16
          items:
            type: string
            minLength: 1
            maxLength: 64
        sanitized_derivative_sha256:
          type: string
          pattern: '^[a-f0-9]{64}$'
    StartImageAnalysisRequest:
      type: object
      additionalProperties: false
      required: [expected_capture_version, sanitization, user_confirmed]
      properties:
        expected_capture_version:
          type: integer
          minimum: 1
        sanitization:
          $ref: '#/components/schemas/PrivacySanitization'
        user_confirmed:
          type: boolean
          const: true
    ImageAnalysisJobReceipt:
      type: object
      additionalProperties: false
      required: [id, capture_id, household_id, child_id, status, attempt, sanitization_schema_version, sanitized_derivative_sha256, created_at, updated_at]
      properties:
        id:
          type: string
          format: uuid
        capture_id:
          type: string
          format: uuid
        household_id:
          type: string
          format: uuid
        child_id:
          type: string
          format: uuid
        status:
          type: string
          enum: [queued, running, succeeded, failed, blocked]
        attempt:
          type: integer
          minimum: 0
        sanitization_schema_version:
          type: string
          const: privacy-sanitization.v1
        sanitized_derivative_sha256:
          type: string
          pattern: '^[a-f0-9]{64}$'
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        extraction_id:
          type: string
          format: uuid
          nullable: true
        error_code:
          type: string
          minLength: 1
          maxLength: 64
          nullable: true
    QuestionExtractionRecord:
      type: object
      additionalProperties: false
      required: [id, capture_id, household_id, child_id, extraction, created_at]
      properties:
        id:
          type: string
          format: uuid
        capture_id:
          type: string
          format: uuid
        household_id:
          type: string
          format: uuid
        child_id:
          type: string
          format: uuid
        extraction:
          $ref: '#/components/schemas/QuestionExtraction'
        created_at:
          type: string
          format: date-time
    VerifyQuestionRequest:
      type: object
      additionalProperties: false
      required: [expected_capture_version, question_text, options, formulas, has_diagram, has_handwriting]
      properties:
        expected_capture_version:
          type: integer
          minimum: 1
        question_text:
          type: string
          minLength: 1
          maxLength: 4000
        options:
          type: array
          maxItems: 20
          items:
            type: string
        formulas:
          type: array
          maxItems: 50
          items:
            type: string
        has_diagram:
          type: boolean
        has_handwriting:
          type: boolean
        answer_text:
          type: string
          maxLength: 1000
          nullable: true
    VerifiedQuestion:
      type: object
      additionalProperties: false
      required: [id, capture_id, extraction_id, version, subject, question_text, options, formulas, has_diagram, has_handwriting, verified_by, verified_at]
      properties:
        id:
          type: string
          format: uuid
        capture_id:
          type: string
          format: uuid
        extraction_id:
          type: string
          format: uuid
        version:
          type: integer
          minimum: 1
        subject:
          type: string
          const: math
        question_text:
          type: string
          minLength: 1
          maxLength: 4000
        options:
          type: array
          maxItems: 20
          items:
            type: string
        formulas:
          type: array
          maxItems: 50
          items:
            type: string
        has_diagram:
          type: boolean
        has_handwriting:
          type: boolean
        answer_text:
          type: string
          maxLength: 1000
          nullable: true
        verified_by:
          type: string
          enum: [child, parent]
        verified_at:
          type: string
          format: date-time
    StartTutorHintRequest:
      type: object
      additionalProperties: false
      required: [verified_question, level, child_id]
      properties:
        verified_question:
          $ref: '#/components/schemas/VerifiedQuestion'
        level:
          type: integer
          minimum: 1
          maximum: 3
        child_id:
          type: string
          format: uuid
    TutorHintResponse:
      type: object
      additionalProperties: false
      required: [schema_version, policy_version, provider, model, level, prompt, next_step, requires_child_response, direct_answer, cost_cents]
      properties:
        schema_version:
          type: string
          const: tutor-hint.v1
        policy_version:
          type: string
          const: offline-tutor-policy.v1
        provider:
          type: string
          const: local-policy
        model:
          type: string
          const: rules-v1
        level:
          type: integer
          minimum: 1
          maximum: 3
        prompt:
          type: string
          minLength: 1
          maxLength: 500
        next_step:
          type: string
          minLength: 1
          maxLength: 500
        requires_child_response:
          type: boolean
          const: true
        direct_answer:
          type: 'null'
        cost_cents:
          type: integer
          const: 0
    OcrJobReceipt:
      type: object
      additionalProperties: false
      required: [id, capture_id, mode, status, attempt, enqueued_at]
      properties:
        id:
          type: string
          format: uuid
        capture_id:
          type: string
          format: uuid
        mode:
          type: string
          enum: [text, formula]
        status:
          type: string
          enum: [queued, running, succeeded, failed]
        attempt:
          type: integer
          minimum: 0
        enqueued_at:
          type: string
          format: date-time
        started_at:
          type: string
          format: date-time
          nullable: true
        finished_at:
          type: string
          format: date-time
          nullable: true
        result_id:
          type: string
          format: uuid
          nullable: true
    EnqueueOcrJobRequest:
      type: object
      additionalProperties: false
      required: []
      properties:
        mode:
          type: string
          enum: [text, formula]
          default: text
    OcrResult:
      type: object
      additionalProperties: false
      required: [id, capture_id, household_id, child_id, provider, model, model_version, schema_version, confidence, status, requires_manual_confirmation, created_at]
      properties:
        id:
          type: string
          format: uuid
        capture_id:
          type: string
          format: uuid
        household_id:
          type: string
          format: uuid
        child_id:
          type: string
          format: uuid
        provider:
          type: string
          minLength: 1
          maxLength: 80
        model:
          type: string
          minLength: 1
          maxLength: 120
        model_version:
          type: string
          minLength: 1
          maxLength: 80
        schema_version:
          type: string
          minLength: 1
          maxLength: 32
        confidence:
          type: number
          minimum: 0
          maximum: 1
        status:
          type: string
          enum: [candidate, empty]
        requires_manual_confirmation:
          type: boolean
          const: true
        created_at:
          type: string
          format: date-time
    OcrCandidate:
      type: object
      additionalProperties: false
      required: [id, result_id, sequence, text, confidence]
      properties:
        id:
          type: string
          format: uuid
        result_id:
          type: string
          format: uuid
        sequence:
          type: integer
          minimum: 1
        text:
          type: string
          minLength: 1
          maxLength: 1000
        confidence:
          type: number
          minimum: 0
          maximum: 1
    OcrResultWithCandidates:
      type: object
      additionalProperties: false
      required: [result, candidates]
      properties:
        result:
          $ref: '#/components/schemas/OcrResult'
        candidates:
          type: array
          items:
            $ref: '#/components/schemas/OcrCandidate'
    CorrectCaptureRequest:
      type: object
      additionalProperties: false
      required: [expected_capture_version, corrected_text]
      properties:
        expected_capture_version:
          type: integer
          minimum: 1
        corrected_text:
          type: string
          minLength: 1
          maxLength: 1000
    ConfirmOcrCandidateRequest:
      type: object
      additionalProperties: false
      required: [expected_capture_version, candidate_id]
      properties:
        expected_capture_version:
          type: integer
          minimum: 1
        candidate_id:
          type: string
          format: uuid
    CaptureCorrection:
      type: object
      additionalProperties: false
      required: [id, capture_id, household_id, child_id, sequence, corrected_text, created_at]
      properties:
        id:
          type: string
          format: uuid
        capture_id:
          type: string
          format: uuid
        household_id:
          type: string
          format: uuid
        child_id:
          type: string
          format: uuid
        sequence:
          type: integer
          minimum: 1
        corrected_text:
          type: string
        created_at:
          type: string
          format: date-time
    RecordAttemptRequest:
      type: object
      additionalProperties: false
      required: [event_id, answer_summary]
      properties:
        event_id:
          type: string
          format: uuid
        answer_summary:
          type: string
          minLength: 1
          maxLength: 200
    Attempt:
      type: object
      additionalProperties: false
      required: [id, event_id, household_id, child_id, session_id, sequence, answer_summary, recorded_at]
      properties:
        id:
          type: string
          format: uuid
        event_id:
          type: string
          format: uuid
        household_id:
          type: string
          format: uuid
        child_id:
          type: string
          format: uuid
        session_id:
          type: string
          format: uuid
        sequence:
          type: integer
          minimum: 1
        answer_summary:
          type: string
        recorded_at:
          type: string
          format: date-time
    SyncEvent:
      type: object
      additionalProperties: false
      required: [event_id, idempotency_key, kind, session_id, answer_summary]
      properties:
        event_id:
          type: string
          format: uuid
        idempotency_key:
          type: string
          minLength: 8
          maxLength: 128
        kind:
          type: string
          const: record_attempt
        session_id:
          type: string
          format: uuid
        answer_summary:
          type: string
          minLength: 1
          maxLength: 200
    SyncBatchRequest:
      type: object
      additionalProperties: false
      required: [schema_version, events]
      properties:
        schema_version:
          type: integer
          const: 1
        events:
          type: array
          minItems: 1
          maxItems: 50
          items:
            $ref: '#/components/schemas/SyncEvent'
    SyncEventResult:
      type: object
      additionalProperties: false
      required: [event_id, status, attempt]
      properties:
        event_id:
          type: string
          format: uuid
        status:
          type: string
          enum: [applied, replayed]
        attempt:
          $ref: '#/components/schemas/Attempt'
    SyncBatchResult:
      type: object
      additionalProperties: false
      required: [schema_version, results]
      properties:
        schema_version:
          type: integer
          const: 1
        results:
          type: array
          items:
            $ref: '#/components/schemas/SyncEventResult'
  responses:
    Unauthorized:
      description: Missing or invalid synthetic principal.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    Forbidden:
      description: Principal role cannot perform this operation.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    NotFound:
      description: Resource is not visible in the caller's household.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    IdempotencyConflict:
      description: Idempotency key was reused with a different payload.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    InvalidRequest:
      description: Request failed schema validation.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    ErrorResponse:
      type: object
      additionalProperties: false
      required: [code, message]
      properties:
        code:
          type: string
        message:
          type: string
