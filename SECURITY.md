# SECURITY.md

## 1. 安全目标与范围

- 保护对象：儿童身份/档案、家庭关系、学习记录、作答与错因、题目图片、AI 输入输出、设备令牌、家长会话、内容授权、导出/删除记录、密钥、备份和审计日志。
- 信任边界：儿童/家长设备 ↔ API；家庭 ↔ 家庭；普通家庭角色 ↔ 内容/管理角色；教材/课程文件 ↔ 解析器/知识发布；原图/本地 PrivacySanitizer ↔ 云视觉/Tutor Provider；API ↔ HMS/对象存储；local/staging ↔ production；在线服务 ↔ 端侧离线队列。
- 攻击者能力假设：恶意互联网用户、被盗设备/令牌、同家庭误操作、试图跨家庭访问的认证用户、恶意文件/Prompt/模型输出、受损第三方服务或依赖、能读取低环境日志的内部人员。
- 安全目标：家庭强隔离、最小权限和最小数据；学习记录完整可追溯；模型输出不被信任；导出/删除真实有效；成本和滥用有界；不泄露儿童数据。
- 合规状态：适用法域、儿童隐私法律依据、监护人同意、数据主体流程和正式保留周期均为 `TBD（安全/法务 Owner 在接入真实儿童数据前批准）`。这是 production 阻塞项。

自用范围说明：项目 Owner 已明确本轮先服务单家庭自用，不做商业化、地区扩展或第三方身份平台；上述合规与监护人流程在自用阶段暂不作为代码开发阻塞，但仍是未来对外部署前的必补项。

### 自用部署修订（2026-07-15）

本产品当前按单家庭自用、自托管方式推进；地区、商业化、第三方 IdP 和复杂监护人流程暂不作为本轮实现门槛。Compose 只使用账号密码和可撤销会话；HMAC、Demo Header、静态 Web Token 和认证模式开关已删除。NewAPI/OpenAI-compatible Adapter 仅在显式配置时启用。Ubuntu 24.04 x86_64 栈已完成健康、迁移、登录/首次改密、真实 NewAPI synthetic 大图和 PostgreSQL/MinIO 隔离恢复验证；浏览器自动 E2E、完整设备生命周期和生产监控仍待执行。

2026-07-17 网络运行边界：远端 VM 已关闭 IPv6，Docker daemon 通过用户提供的 SOCKS5 出网代理拉取镜像；项目 `.env` 仅存在远端、权限为 600，未进入仓库。NewAPI key 只通过远端环境注入；联调只发送运行时生成的 synthetic 图片，没有发送真实儿童图片或把密钥写入输出。

账号密码目标保留自用的简单边界：不接入短信、邮箱、社交登录、OIDC 或 MFA。项目 Owner 已授权 LAN 首次引导，因此安全性由“仅空库初始化一次 + 首次登录强制改密 + 改密前阻断全部家庭数据 + 登录限速/锁定 + 可撤销会话 + Household/角色授权”提供。`admin/admin123456` 不是长期凭据；仅用于首次改密，改密后立即失效，且不得暴露到公网。

当前可运行边界是：`ImageAnalysis` 回执经安全校验后进入 queued，worker 读取私有对象、调用已配置的单一 NewAPI，并只保存 Schema 校验后的未确认 `QuestionExtraction`；人工确认生成 `VerifiedQuestion`，Tutor 只按服务端 ID 读取并追加写 `TutorTurn`，派生对象在成功/失败后清理。真实 NewAPI synthetic 和备份恢复已完成；真实自动视觉检测器和生产监控仍未完成，不得把规则/手工遮挡描述为绝对匿名。

Apple Silicon 的默认 Compose 镜像是 Linux ARM64 调试运行时；由于锁定的 PaddlePaddle 3.3.1 只提供 macOS ARM64 和 Linux x86_64 wheel，该镜像不含 Paddle OCR。不得把规则/手工遮挡或“ARM 镜像可以启动”解释成完整 PrivacySanitizer 已通过；启用图片外发前仍必须在具备获批本地检测器的运行时完成固定脱敏评测和用户确认门禁。

当前实现状态：Profile/Device、Learning/Capture/OCR/ImageAnalysis、Account/AuthSession、TutorTurn、教材知识图谱和短期 Export 均有 PostgreSQL 仓储；本地与 Ubuntu OpenAPI `0.11.0`/迁移 `0025`，孩子绑定拍题/活动会话、家长任务/周报/导出/删除、Compose 和反向 Household 授权测试已存在。孩子档案删除按依赖清理学习、Capture/OCR、视觉、Tutor、教材派生数据和导出数据并级联账号/会话；Flutter 令牌只进系统安全存储，离线 Attempt 只进按账号隔离的 SQLite。真实视觉检测器和真实教材 Provider 质量/成本验收仍未完成。

ADR-0020/0023 与 PLAN-0017/0018 已进入实现：`0020` 把视觉四态候选、置信度、可见作答步骤和人工确认绑定到 VerifiedQuestion；`0021`～`0025` 又补齐 closeout、ReviewAttempt、PDF 私有原页、分批多模态教材理解、整本知识图谱、Tutor 递进披露和来源任务。L1/L2/完整解答只接收已确认题目文字与最小已批准教材知识片段，不发送图片、对象键或整本 PDF；教材分析是单独的有界流程，只对家长声明无儿童姓名/个人批注的教材页派生图进行最多 4 页一批的单 Provider 分析。任务 planner 只接收有界候选和不透明来源键，未知来源计划整体拒绝。当前仍需真实设备、Provider、固定云端质量/成本 eval 和发布门禁，不能把代码测试通过误报为完整公网生产门禁。

## 2. 数据分类

| 等级 | 示例 | 允许存储 | 允许日志 | 传输/静态加密 | 保留 |
| --- | --- | --- | --- | --- | --- |
| Public | 开源代码、公开文档、已授权公开内容 | Git/公开站点 | 可记录非敏感元数据 | 传输 TLS；静态按平台 | 按项目生命周期 |
| Internal | 架构草案、合成测试数据、非敏感指标、内部事件名 | 受控仓库/开发系统 | 可记录必要字段 | 传输 TLS；生产静态加密 | 按协作需要，定期清理 |
| Confidential | 家庭关系、孩子档案、学习记录、错题、周报、家庭导入内容、去标识 AI 摘要 | 仅批准的业务库/设备安全存储 | 仅去标识/不可逆标识和最小字段 | 传输和静态均必须加密 | `TBD`；与导出/删除策略一致 |
| Restricted | 原始题目/作答图片、临时脱敏副本、密码哈希、会话原值/摘要、设备凭据、密钥、原始 AI 输入输出中的儿童信息、生产转储/备份 | 批准的密钥管理器、对象存储、业务库或设备安全区；脱敏副本只在受控临时存储/内存；会话原值只到客户端安全存储 | 永不记录原值 | 传输和静态必须加密，访问最小化 | 最短必要；脱敏副本成功后立即删除、失败最多 24 小时；会话最长 30 天并可即时撤销；其他图片/备份见批准策略 |

家庭导入的教材/课程原文同时受 Confidential 与版权/授权控制：只允许家庭私有对象存储和获批解析流程，不进入公共题库、仓库、测试/评测集或普通数据导出。教材理解可将无个人信息的页级派生图和本地文字按最多 4 页一批发送给单一获批 Provider，并仅将经过 Schema/来源校验的结构化结果入库；后续 Tutor/推荐只有在来源/版本明确且当前请求确有必要时才可发送最小已批准知识片段。

任何无法分类的数据按更高一级处理。把 Confidential/Restricted 数据降级必须有文档化评审。

## 3. 认证与授权

- 实现状态：Compose 只运行 ADR-0017 的账号密码和可撤销会话；HMAC、Demo Header 和免登录旁路已删除，不存在迁移期兼容开关。
- 身份模型：`parent_admin` 账号拥有 Household 管理权限；`child` 账号必须绑定同一 Household 的一个 ChildProfile。自用版不强制手机号，也不提供公开注册。
- 凭据：密码使用 Argon2id 哈希，数据库不保存明文或可逆密码；普通家长新密码 12～128 字符、孩子密码 8～128 字符。用户名在 Household 内进行大小写不敏感唯一化；登录失败统一返回不可枚举错误。
- 初始化：仅空账号库可创建一次 `admin/admin123456`，在运行时立即保存为 Argon2id 哈希并标记 `must_change_password=true`。项目 Owner 已授权该凭据可在受信家庭局域网完成首次登录；改密前仅允许查询当前账号、改密和退出，其他家庭数据接口返回稳定 `password_change_required`。改密后撤销全部引导会话并签发新会话；该临时凭据不得暴露到公网。
- 登录防护：同一账号连续 5 次失败锁定 15 分钟，并对账号/IP 组合做有界限速；登录、锁定、阻断和失败已写入现有审计表的稳定字段，不记录用户名明文、密码或会话值。
- 会话：服务端签发至少 256 bit 随机不透明值，数据库只保存 SHA-256 摘要；最长有效期 30 天。登出、改密、管理员重置/停用账号时撤销相关会话；不存在无法撤销的长期客户端 Token。
- 客户端存储：Web 使用 `HttpOnly`、`SameSite=Lax` Cookie，TLS 下强制 `Secure`，所有状态变更接口验证 CSRF；Flutter 在登录前只允许无 user-info/查询/片段/路径的 HTTP(S) 服务端地址，地址变更先清除旧会话，会话放入 Keychain/Android Keystore，不进 URL、日志、普通 SQLite 或构建参数。LAN 调试可使用 HTTP，公网/生产必须使用 HTTPS。
- 授权模型：每个业务资源带 Household 归属；服务端逐次校验 account、session、household、role 和 ChildProfile 绑定。仅依赖客户端隐藏按钮不算授权。
- 孩子聚合：Web 可以把档案与账号呈现为一个孩子，但服务端仍分离 `ChildProfile` 与 `Account` 的存储和权限。聚合创建必须由已认证家长通过一个幂等事务完成，任一校验/唯一冲突失败整体回滚；响应和日志不得包含初始密码、密码哈希、会话材料或儿童姓名。现有绑定审计完成后，一个 ChildProfile 最多允许一个 child Account。
- 孩子权限：只能读取/完成分配给自己的任务、提交作答/捕获、请求允许级别提示和读取必要反馈；不能创建账号、修改家庭、策略、成本、保留或管理内容。
- 多孩子选择：家长工作台的 `child_id` 查询参数、Cookie 或本地选择状态均是不可信输入。API 必须重新验证其属于当前 Household；家长只能选择本家庭孩子，孩子账号无论怎样篡改选择都只能读取自己的绑定档案。任务与周报必须服务端过滤，禁止只靠前端隐藏兄弟姐妹数据。
- 管理操作：家长可在 Web 创建、停用、启用和重置同一 Household 的孩子账号。内容发布、策略/模型切换、数据导出/删除、账号重置和高成本配置要求在 10 分钟内重新验证家长密码并审计。
- 恢复与二次验证：唯一管理员忘记密码时，只提供服务器本机、显式执行且有审计的恢复命令；自用版明确不接入短信、邮箱、社交登录、OIDC 或 MFA。若未来开放公网或多家庭，必须重新威胁建模并新写 ADR。
- 测试：覆盖正确/错误密码、用户枚举、5 次失败锁定、引导凭据 LAN 首次登录与数据阻断、Cookie/CSRF、Flutter 安全存储边界、会话过期/撤销、同家庭允许、不同家庭拒绝、角色不足拒绝、孩子档案反向绑定、聚合创建原子回滚/幂等/并发唯一、多孩子选择 ID 篡改和兄弟姐妹隔离、枚举 ID 防护，以及审计不含密码/令牌/用户名/儿童姓名。

## 4. 输入、输出、文件与 AI

- 所有外部输入在信任边界按版本化 Schema 验证，包括 OpenAPI 参数、离线事件、OCR 文本、AI JSON、导入内容和第三方回调。教材上传合同和当前 API/Web 都只允许 PDF；API 拒绝非 `.pdf` / `application/pdf` / `%PDF-` 结构并返回稳定错误，再对 PDF 增量计算 SHA-256 后写入私有 MinIO。
- 教材导入必须在读取内容前校验 Session/家长角色/Household/孩子、扩展名/MIME/文件头/实际 PDF 结构、大小/页数、SHA-256、来源授权声明和“不含儿童姓名/个人批注/其他个人信息”声明；Accepted ADR-0021/0023 的解析在 Compose internal `parser-backend` 网络中运行，使用锁定的 `pdfplumber/pdfminer-six/pypdfium2`，并限制 50 MB、400 页、单页文字量、预览图 2 MiB、Provider 单图和整本归纳输入，阻断加密、危险动作/链接和嵌入附件。无文字层页面仍保留私有原页并走多模态分析，不得把 OCR/文字提取完整度冒充语义置信度；grounding 只读取已发布 Snapshot 和已批准知识图谱的最小片段。
- 材料文字、页图、元数据、目录和注释都属于不可信内容。任何“忽略规则/泄露 Prompt/调用工具”等文档指令只能作为教材数据，不得成为系统/开发者指令；页级与整本模型输出必须经过固定 allowlist Schema、页码/来源键/全书缺页校验和家长审核批准。
- 数据库使用参数化访问；排序/字段选择/过滤白名单化；禁止字符串拼接查询。
- 文件上传只允许业务批准的 JPEG/PNG。当前本地实现已要求 App 携带 Session 只上传到 API：API 在读取字节前校验 Household/角色/孩子/StudySession，以固定块和实际累计值限制 1–8 MB，增量计算 SHA-256，验证声明 MIME、容器魔数、宽高/像素、完整像素解码，并通过内部地址写入随机 staging 对象；失败/断连/超时必须中止 multipart 或删除 staging，禁止无界读取完整请求体。客户端不得获得对象键、预签名 URL、MinIO 地址或凭据，Compose 本地与 Ubuntu MinIO `9000` 均不向宿主/LAN 暴露。超过 NewAPI 请求预算的已确认脱敏副本继续在内存中去元数据、等比缩放和有界 JPEG 重编码，不落盘且不会恢复遮挡像素。
- 不从用户提供 URL 直接抓图。若未来支持 URL 导入，默认拒绝私网/本机/云元数据/重定向逃逸，限制 DNS、协议、端口、大小和超时，并单独威胁建模。
- Web 输出使用框架自动转义，富文本白名单清洗；CSP、安全头、Cookie 属性和 CORS 白名单在 Web 脚手架时落地并测试。
- 图片外发前必须经过 ADR-0015 的三层门禁：App 单题裁剪和预览；服务端完整像素校验/元数据清除/本地自动脱敏；用户查看当前脱敏副本并选择确认、重新裁剪或手动涂抹。自动脱敏不是匿名保证，`safe_to_upload=false`、大面积真实人脸、身份证信息、无法安全裁剪、规则版本失效或用户未确认时必须阻断外发。
- 本地 `PrivacySanitizer` 中的 OCR 只允许输出敏感标签/文本框位置和置信度，结合上下文规则、人脸、二维码和条形码检测；不得把 OCR 题目全文作为最终解析。普通数学算式/解题笔迹是错因分析必要输入，不得仅因为是手写而自动遮挡；姓名、班级、学号、联系方式等敏感笔迹仍必须扩大边距后用实色覆盖并重新编码为全新像素。禁止仅模糊/马赛克、只保存遮挡坐标或把原文件与掩码同时发送。
- 云视觉输出不可信：Provider Adapter 只接收用户确认的脱敏字节和最少上下文，输出固定 `QuestionExtraction` Schema；Schema 必须将题目和孩子作答分开，并输出作答区覆盖、作答步骤候选、`worked/blank/unclear/answer_area_missing` 和置信度。原图、MinIO URL、对象键和敏感 OCR 文本不得进入请求。单次解析只使用一个获批 Provider，失败不得自动向多个 Provider 广播；所有题目/作答结果必须人工确认或修正，才能进入 Tutor、错因或掌握度流程。
- 错题讲解必须同时验证 VerifiedQuestion、当前孩子、已确认 AttemptEvidence 和已发布 CurriculumSnapshot。`worked` 证据用于定位错误步骤；答题区可见且用户确认的 `answer_state=blank` 是有效证据，可进入从头讲解。`unclear/answer_area_missing` 不得自动当空白，只能请求重拍或手工确认。错误类型须区分孩子自述、规则结果和模型建议，禁止把模型猜测伪装成孩子事实。
- Tutor 只检索当前 Household/Child/Subject/Assignment 的已发布、已批准知识片段，并传递最小引用；禁止发送教材页图/整本教材、未批准草稿、其他孩子材料或无关历史。输出需经过 mode/Schema、来源、答案泄露、重复和题意相关校验；L2 必须绑定同会话 L1 并增加有限脚手架，L1/L2 不得返回最终答案/完整步骤。失败安全回退到题型相关本地提示，不创建标准答案或掌握事实。
- ReviewSchedule 和 TaskRecommendation 是服务端可解释派生状态：AI 与客户端都不得直接写复习到期、永久掌握或正式 Task。讲解 closeout 必须以 VerifiedQuestion/已确认 AttemptEvidence 原子创建错题；ReviewPolicy 更新必须基于服务端判定的追加写 ReviewAttempt，不能继续信任客户端自报 `correct`。推荐先在家庭边界遍历全部开放错题和已批准知识图谱的具体练习，只把最多 30 个候选及来源键交给单一 NewAPI；解析回本地知识点、题目、视觉说明与页码后再经家长批准创建 Task，模型不得改写题目、引用未知来源、忽略已有来源或突破每日 3 项上限。
- 当前 PaddleOCR 完整解析代码和五模型供应链是被 ADR-0015 替代路线的实现事实，仍须保持构建期 HTTPS 来源/SHA-256、运行时不下载和可回滚约束；迁移后应只保留经评测的轻量隐私检测模型，模型/依赖变化必须另行审查。具体云 Provider、训练退出/不用于训练、保留、区域、子处理方、预算和密钥尚未批准，任何真实图片外发保持关闭。
- Prompt 只传递完成当前请求所需的最少数据，优先使用去标识摘要；图片解析只传脱敏副本，不传 OCR 检出的敏感文字；禁止把密钥、长期身份、无关家庭历史、对象 URL、原始图片或原始日志发送给 Provider。
- Provider 错误、响应和调试信息必须脱敏；禁止在用户错误信息中暴露堆栈、内部 URL、模型密钥或策略全文。

## 5. 密钥与配置

- 生产密钥存储：`TBD（云/自托管平台决定后选择批准的 Secret Manager）`。
- 本地开发：只使用无生产权限的测试凭据，通过未提交的环境文件或本地密钥存储注入；仓库只提交脱敏 `.env.example`。
- 轮换：Provider、对象存储、数据库、签名和推送凭据的周期 `TBD`；泄露或人员/设备变更时必须立即轮换并撤销。
- 环境隔离：local/staging/production 使用不同账户、bucket、数据库、密钥和回调地址；生产凭据不得在本地/CI PR 作业可读。
- 禁止在代码、提交、日志、截图、错误、Prompt、测试夹具、评测集和构建产物中出现真实凭据或 Restricted 数据。
- P0 必须建立提交前/CI 密钥扫描；具体工具和阻断规则 `TBD`。

## 6. 依赖与供应链

- 允许包源：Dart/Flutter 官方生态、npm 官方或批准镜像、PyPI 官方或批准镜像、受信容器注册表；新增私有源需 ADR 和凭据隔离。
- 锁文件：Flutter 提交 `pubspec.lock`，Web 提交 `pnpm-lock.yaml`，API 提交 `uv.lock`，容器使用不可变 digest 或受控版本；同一子项目不得存在冲突锁文件。
- 依赖准入：说明用途、许可证、维护状态、替代方案、权限、体积/成本和已知漏洞；核心依赖/Provider 需要 ADR。
- 漏洞扫描：具体命令 `TBD（代码和镜像建立后写入 TESTING.md/CI）`；合并/发布前扫描直接依赖、传递依赖、容器和密钥。
- 修复 SLA：Critical 在确认后立即阻断发布并启动处置；High 默认 7 天内修复或由 Owner 书面接受临时缓解；Medium/Low SLA `TBD`。
- 构建：CI 使用最小权限、固定 action/镜像版本、受保护发布凭据；生产前决定 SBOM、来源证明和签名策略。

## 7. 日志与审计

- 必须审计：登录/失败、设备注册/撤销、家庭角色变化、材料导入/解析/发布/撤销、任务建议审批、任务/会话关键状态、错题/复习状态变化、同步冲突、Capture/PrivacySanitizer/图片解析、Tutor 请求和策略/教材快照版本、导出/删除、密钥/配置/模型变更、备份恢复。
- 当前认证实现已覆盖：成功/失败/锁定/阻断登录、改密成功/失败、再认证失败、登出、孩子账号创建、启停和重置；`audit_events` 只保存稳定事件名、Household/资源 UUID 和时间。
- PLAN-0013 实施后，孩子聚合创建应使用一个稳定审计事件记录 actor、Household、生成的 Profile/Account UUID 和结果，不记录表单中的用户名、姓名或初始密码；多孩子切换属于显示偏好，不记录儿童姓名或产生高频敏感审计。
- 稳定字段：event name、timestamp、trace/request、actor/device/household 的不可逆标识、resource type/id、action、result/reason、sanitizer/rule/policy/schema/model version、检测类型枚举与数量、用户确认、脱敏副本不可逆哈希、删除状态、延迟和成本（适用时）。
- 禁止字段：密码/PIN、密码哈希、会话原值/摘要、令牌、签名 URL、密钥、原始图片、教材原文/完整片段、完整题目/作答/错误步骤、原始 Prompt/模型输出、儿童姓名/手机号/精确身份。
- 脱敏：默认 denylist + allowlist 双重控制；自由文本异常先清洗；生产日志采样不得绕过安全事件。
- 保留与访问：按最小权限和审计用途设定；精确期限、日志平台和访问审批 `TBD（生产前批准）`。
- 告警：跨家庭拒绝/成功异常、管理员操作、设备爆破、上传滥用、AI Schema/安全失败、成本突增、删除/备份失败和密钥扫描命中。阈值 `TBD`。

## 8. 数据生命周期

- 创建：只收集完成明确产品目的需要的字段；ChildProfile 不收集无关身份或联系方式。
- 教材与知识：原材料、私有原页预览、页级分析、知识图谱草稿、已批准图谱和已发布 Snapshot 分层保存；精确保留期 `TBD`。撤销授权或删除材料时立即阻止新检索/推荐，并按引用策略删除原文、预览对象与可重算草稿；既有学习事实只保留最小来源标识/版本，不复制整段教材。备份中的处理必须在真实材料导入前批准。
- 错题与复习：MistakeRecord、AttemptEvidence（包括已确认的 `answer_state=blank`）、TutorTurn 和复习 Attempt 随孩子数据导出/删除；导出可包含家庭自己的结构化错题与作答状态，但不得包含教材原文件、对象键、Provider 原始响应或密码/会话。ReviewSchedule/Recommendation 可重算，删除源事实后必须清理。
- 使用：训练/改进模型、人工审阅或跨家庭分析不得默认为原用途；需要单独批准、告知和最小化。
- 图片：原图默认 24 小时；OCR 失败最多保留 7 天；裁剪单题默认 30 天（ADR-0011）。DataLifecycle worker 清理到期 Capture 对象；删除孩子档案在对象级联成功后按依赖删除 PostgreSQL 学习/视觉/Tutor/导出数据并级联账号/会话。备份中的删除遵循备份到期/轮换，不能承诺即时物理擦除。对象键不含身份，图片默认不用于训练。
- 脱敏副本：只用于一次获批云视觉解析，不允许家长长期保存；成功或最终失败后立即删除，同一 Provider 的有界重试/人工检查最多保留 24 小时，不进入业务备份。删除状态必须可审计，但日志不得包含副本、掩码、对象键或敏感 OCR 文本。
- 导出：家长会话发起并受 Household/角色约束；服务端生成 24 小时不可变 JSONB 快照，幂等重放返回同一内容，生命周期 worker 删除过期快照。导出不包含密码、会话、对象键、图片字节或 Provider 原始响应。
- 删除：定义 requested → verified → processing → completed/failed 状态；删除孩子档案时级联覆盖数据库、原图、裁剪图、对象元数据、缓存、搜索向量和适用备份策略；失败不得报告完成。
- 备份：加密、最小权限、恢复演练；删除在备份中的处理（立即擦除或到期不可恢复）须在真实数据进入前批准并告知。

## 9. 安全发布门槛

- [ ] 家庭/角色/设备权限有正向和反向测试，无 ID 枚举或跨家庭访问。
- [ ] 教材导入覆盖文字/扫描/图形/加密/损坏/超限 PDF、扩展名/MIME/文件头伪造、Word/PPT/Excel 双端拒绝、PDF 对象/流资源炸弹、危险动作/链接/附件、解析器出网/资源上限、授权与无个人信息声明、页图单 Provider/批次/输入/成本上限、Prompt 注入、页码/练习来源键/全书缺页、知识图谱批准/发布隔离、跨家庭、版本和原件/预览撤销删除。
- [ ] 错题完整讲解覆盖 VerifiedQuestion/AttemptEvidence/Snapshot 三重门禁；覆盖有作答、真实空白、浅色字迹/擦改、答题区未入镜/不清和人工修正；覆盖有作答定位错步、空白从头讲、模式绕过、错版/超纲、来源缺失、算术/单位校验、错误原因误推断和最小 Provider 输入；失败不创建标准答案/掌握事实。
- [ ] 复习和任务覆盖 closeout 原子性、客户端伪造结果、ReviewAttempt 追加写、确定性 Policy、并发/幂等、AI/客户端不可直接写到期/掌握/Task、家长审批、每日上限和跨孩子隔离。
- [x] ADR-0017 API 认证回归通过：空库仅初始化一次、默认凭据仅限受信 LAN 的首次改密、首次改密前数据阻断、Argon2id 参数受控、登录锁定、统一错误、会话撤销和孩子账号反向绑定；Web Cookie/CSRF、Flutter 安全存储和真实设备生命周期仍待环境验收。
- [ ] 外部输入、文件、URL（若有）、离线事件、AI 输出和错误路径已测试。
- [ ] 脱敏固定集覆盖敏感信息漏检/误遮挡、EXIF/缩略图/原始像素清除、实色遮挡、人脸/二维码/条形码、手动涂抹、确认哈希绑定、单 Provider 请求和临时副本删除；任何未确认/不安全图片均无法外发。
- [ ] 无明文密钥、真实儿童数据或 Restricted 内容泄漏；日志字段通过 allowlist/脱敏测试。
- [ ] 依赖、容器和构建链扫描无未批准 Critical/High 问题。
- [ ] 模型/Prompt/Policy/Schema/Snapshot 版本可追溯；L1/L2 重复/越级/答案泄露、低置信度、作答状态未确认、未入镜误判空白、模式越权、完整讲解错误和敏感内容测试通过。
- [x] 图片保留清理器、家庭导出/删除和备份恢复已完成自动或 synthetic 演练；备份删除采用到期轮换，自动视觉检测器仍是单独阻塞项。
- [ ] 成本/滥用、授权异常、备份/删除失败告警已演练。
- [ ] 适用法域、监护人同意、保留期限、数据处理方和安全联系渠道已批准。

## 10. 漏洞与事件响应

- 报告渠道：`TBD（项目 Owner 在首次公开/试用前建立安全邮箱或私密渠道）`。
- 分级：Critical（跨家庭/密钥/大量 Restricted 数据/远程控制）、High（单家庭敏感泄露、权限提升、记录完整性破坏）、Medium、Low；最终模型和 SLA 由安全 Owner 批准。
- 隔离/缓解：撤销令牌/密钥、关闭功能开关或 Provider、隔离上传/队列、阻断发布、保留证据；任何删除证据或通知外部人员需授权。
- 通知：适用法律、用户通知和第三方联络由安全/法务 Owner 决定；不得自行对外披露儿童数据事件。
- 复盘：记录时间线、影响范围、根因、检测/响应差距、修复、数据处置和防复发；高风险问题需回归测试和 ADR/Runbook 更新。

## 11. 未决安全事项

| 项目 | Owner | 截止条件 | 影响 |
| --- | --- | --- | --- |
| 适用法域、监护人同意和隐私条款 | 安全/法务 Owner | 任何真实儿童数据进入前 | production 阻塞 |
| ADR-0017 账号/会话环境验收、管理员本机恢复和公开暴露边界 | 技术 + 安全 Owner | 自用 Web 在局域网承载真实家庭数据前 | HMAC/Demo 已删除；由 `TASK-0007`/`PLAN-0008` 跟踪真实 PostgreSQL、浏览器和设备生命周期 |
| MFA、外部恢复和多家庭 IdP | 技术 + 安全 Owner | 未来公网开放或多家庭使用前 | 明确不在当前自用范围；届时需重新 ADR/威胁建模 |
| 真实数据法域、备份与正式告知 | 安全/产品 Owner | 任何真实儿童图片/生产前 | 删除和告知阻塞；默认图片保留值见 ADR-0011 |
| Secret Manager、加密和环境拓扑 | 运维 + 安全 Owner | ADR-0008 批准、staging 前 | 部署阻塞 |
| 自用 NewAPI 的模型、响应 Schema、保留/删除行为和停用开关 | 技术 Owner | 实际 NewAPI 联调和真实图片试用前 | 只允许本地自托管；不满足时保持 disabled |
| ADR-0021/0023 PDF 解析、多模态 Provider、资源/成本上限、版权声明和原文保留 | 技术 + 安全 Owner | PLAN-0018 M7 发布前 | 本地依赖、synthetic PDF、Schema/来源、`0025` 和 Ubuntu 前滚/隔离备份恢复已验证；继续完成教材个人信息自动门禁、镜像/SBOM、真实 118 页 PDF、Provider 费用/质量和删除验收 |
| 错题 closeout、Tutor 渐进提示和 ReviewPolicy v2 | 产品 + 技术 Owner | PLAN-0016 M5 家庭使用前 | 代码和服务端判定已实现；固定数学 eval、L1/L2 答案泄露/递进、设备和 1/3/7/14/30 天验收仍需完成 |
| 安全扫描、SBOM、签名和报告渠道 | 技术负责人 | 首次公开/发布前 | 供应链与响应能力 |
