# SECURITY.md

## 1. 安全目标与范围

- 保护对象：儿童身份/档案、家庭关系、学习记录、作答与错因、题目图片、AI 输入输出、设备令牌、家长会话、内容授权、导出/删除记录、密钥、备份和审计日志。
- 信任边界：儿童/家长设备 ↔ API；家庭 ↔ 家庭；普通家庭角色 ↔ 内容/管理角色；API ↔ AI/OCR/HMS/对象存储；local/staging ↔ production；在线服务 ↔ 端侧离线队列。
- 攻击者能力假设：恶意互联网用户、被盗设备/令牌、同家庭误操作、试图跨家庭访问的认证用户、恶意文件/Prompt/模型输出、受损第三方服务或依赖、能读取低环境日志的内部人员。
- 安全目标：家庭强隔离、最小权限和最小数据；学习记录完整可追溯；模型输出不被信任；导出/删除真实有效；成本和滥用有界；不泄露儿童数据。
- 合规状态：适用法域、儿童隐私法律依据、监护人同意、数据主体流程和正式保留周期均为 `TBD（安全/法务 Owner 在接入真实儿童数据前批准）`。这是 production 阻塞项。

当前实现状态：已有合成 Profile/Device API，以及可选 PostgreSQL Learning/Capture（Task/Session/Attempt/CaptureCorrection）仓储、OpenAPI、Flutter 待同步队列边界、锁文件、Compose/CI 草案和反向 Household 授权测试；Capture 仅接收受限媒体声明，人工校正文本不写入审计。demo headers（含 synthetic child binding）和 Profile/Device 内存仓储只限 local/CI，不是认证或生产授权控制。ADR-0004～0006、0008 已 Accepted，但真实儿童数据、媒体上传、Provider 与 production 前置条件仍未满足。

## 2. 数据分类

| 等级 | 示例 | 允许存储 | 允许日志 | 传输/静态加密 | 保留 |
| --- | --- | --- | --- | --- | --- |
| Public | 开源代码、公开文档、已授权公开内容 | Git/公开站点 | 可记录非敏感元数据 | 传输 TLS；静态按平台 | 按项目生命周期 |
| Internal | 架构草案、合成测试数据、非敏感指标、内部事件名 | 受控仓库/开发系统 | 可记录必要字段 | 传输 TLS；生产静态加密 | 按协作需要，定期清理 |
| Confidential | 家庭关系、孩子档案、学习记录、错题、周报、家庭导入内容、去标识 AI 摘要 | 仅批准的业务库/设备安全存储 | 仅去标识/不可逆标识和最小字段 | 传输和静态均必须加密 | `TBD`；与导出/删除策略一致 |
| Restricted | 题目/作答图片、设备/会话令牌、密钥、原始 AI 输入输出中的儿童信息、生产转储/备份 | 批准的密钥管理器、对象存储、业务库或设备安全区 | 永不记录原值 | 传输和静态必须加密，访问最小化 | 最短必要；图片/令牌/备份精确期限 `TBD` |

任何无法分类的数据按更高一级处理。把 Confidential/Restricted 数据降级必须有文档化评审。

## 3. 认证与授权

- 身份模型：家长账户拥有 Household；孩子使用 PIN/设备令牌，不强制手机号；内容维护/管理员是单独高权限角色。
- 身份提供方：`TBD（ADR-0005 决定自建或外部 IdP）`。选择前不得接入真实账户。
- 会话/令牌：短期访问令牌 + 可撤销设备凭证为目标；精确 TTL、刷新、绑定、轮换和丢失设备恢复 `TBD`。令牌只存平台安全存储，不进 URL、日志或普通 SQLite 表。
- 授权模型：每个业务资源带 Household 归属；服务端逐次校验 actor、device、household、role 和操作。仅依赖客户端隐藏按钮不算授权。
- 孩子权限：只能读取/完成分配任务、提交作答/捕获、请求允许级别提示和读取必要反馈；不能修改家庭、策略、成本、保留或管理内容。
- 服务间认证：模块化单体内以进程边界和模块接口控制；未来拆服务必须使用短期工作负载身份和最小权限，不共享万能静态密钥。
- 管理操作：内容发布、策略/模型切换、数据导出/删除、设备撤销和高成本配置需要重新验证身份、审计和必要的双重确认；是否启用 MFA `TBD`。
- 测试：每个资源至少有同家庭允许、不同家庭拒绝、角色不足拒绝、设备撤销/过期拒绝和枚举 ID 防护测试。

## 4. 输入、输出、文件与 AI

- 所有外部输入在信任边界按版本化 Schema 验证，包括 OpenAPI 参数、离线事件、OCR 文本、AI JSON、导入内容和第三方回调。
- 数据库使用参数化访问；排序/字段选择/过滤白名单化；禁止字符串拼接查询。
- 文件上传只允许业务批准的 JPEG/PNG；实现时校验声明类型、魔数、解码结果、尺寸和大小，去除不需要的 EXIF。ADR-0010 确定本地 MinIO 私有 Bucket、随机对象键和短期预签名 URL；客户端不持有存储密钥。精确 TTL、CORS、尺寸限制和 SDK 在实现前锁定并测试。
- 不从用户提供 URL 直接抓图。若未来支持 URL 导入，默认拒绝私网/本机/云元数据/重定向逃逸，限制 DNS、协议、端口、大小和超时，并单独威胁建模。
- Web 输出使用框架自动转义，富文本白名单清洗；CSP、安全头、Cookie 属性和 CORS 白名单在 Web 脚手架时落地并测试。
- OCR/AI 输出不可信：默认本地 PaddleOCR（ADR-0012）必须通过 Schema、敏感内容和置信度检查；所有 OCR 结果都要求人工确认，低置信度/失败要求用户校正；不得直接作为标准答案或掌握度事实。商业 Provider 默认关闭，外部成本上限为 0 元。
- Prompt 只传递完成当前请求所需的最少数据，优先使用去标识摘要；禁止把密钥、长期身份、无关家庭历史或原始日志发送给 Provider。
- Provider 错误、响应和调试信息必须脱敏；禁止在用户错误信息中暴露堆栈、内部 URL、模型密钥或策略全文。

## 5. 密钥与配置

- 生产密钥存储：`TBD（云/自托管平台决定后选择批准的 Secret Manager）`。
- 本地开发：只使用无生产权限的测试凭据，通过未提交的环境文件或本地密钥存储注入；仓库只提交脱敏 `.env.example`。
- 轮换：Provider、对象存储、数据库、签名和推送凭据的周期 `TBD`；泄露或人员/设备变更时必须立即轮换并撤销。
- 环境隔离：local/staging/production 使用不同账户、bucket、数据库、密钥和回调地址；生产凭据不得在本地/CI PR 作业可读。
- 禁止在代码、提交、日志、截图、错误、Prompt、测试夹具、评测集和构建产物中出现真实凭据或 Restricted 数据。
- P0 必须建立提交前/CI 密钥扫描；具体工具和阻断规则 `TBD`。

## 6. 依赖与供应链

- 允许包源：Dart/Flutter 官方生态、npm 官方或批准镜像、PyPI 官方或批准镜像、受信容器注册表；新增私有源需 ADR 和凭据隔离。
- 锁文件：Flutter 提交 `pubspec.lock`，Web 提交 `pnpm-lock.yaml`，API 提交 `uv.lock`，容器使用不可变 digest 或受控版本；同一子项目不得存在冲突锁文件。
- 依赖准入：说明用途、许可证、维护状态、替代方案、权限、体积/成本和已知漏洞；核心依赖/Provider 需要 ADR。
- 漏洞扫描：具体命令 `TBD（代码和镜像建立后写入 TESTING.md/CI）`；合并/发布前扫描直接依赖、传递依赖、容器和密钥。
- 修复 SLA：Critical 在确认后立即阻断发布并启动处置；High 默认 7 天内修复或由 Owner 书面接受临时缓解；Medium/Low SLA `TBD`。
- 构建：CI 使用最小权限、固定 action/镜像版本、受保护发布凭据；生产前决定 SBOM、来源证明和签名策略。

## 7. 日志与审计

- 必须审计：登录/失败、设备注册/撤销、家庭角色变化、任务/会话关键状态、同步冲突、Capture/OCR、Tutor 请求和策略版本、内容/管理操作、导出/删除、密钥/配置/模型变更、备份恢复。
- 稳定字段：event name、timestamp、trace/request、actor/device/household 的不可逆标识、resource type/id、action、result/reason、policy/schema/model version、延迟和成本（适用时）。
- 禁止字段：密码/PIN、令牌、签名 URL、密钥、原始图片、完整题目/作答、原始 Prompt/模型输出、儿童姓名/手机号/精确身份。
- 脱敏：默认 denylist + allowlist 双重控制；自由文本异常先清洗；生产日志采样不得绕过安全事件。
- 保留与访问：按最小权限和审计用途设定；精确期限、日志平台和访问审批 `TBD（生产前批准）`。
- 告警：跨家庭拒绝/成功异常、管理员操作、设备爆破、上传滥用、AI Schema/安全失败、成本突增、删除/备份失败和密钥扫描命中。阈值 `TBD`。

## 8. 数据生命周期

- 创建：只收集完成明确产品目的需要的字段；ChildProfile 不收集无关身份或联系方式。
- 使用：训练/改进模型、人工审阅或跨家庭分析不得默认为原用途；需要单独批准、告知和最小化。
- 图片：原图默认 24 小时；OCR 失败可保留最多 7 天；裁剪单题默认 30 天（ADR-0011）。家长可主动保存或立即删除；对象键不含身份；删除任务必须覆盖派生缩略图/缓存。图片默认不用于训练。
- 导出：家长重新验证身份后发起；异步生成短期下载；下载和过期均审计；不得包含其他家庭数据或内部密钥。
- 删除：定义 requested → verified → processing → completed/failed 状态；删除孩子档案时级联覆盖数据库、原图、裁剪图、对象元数据、缓存、搜索向量和适用备份策略；失败不得报告完成。
- 备份：加密、最小权限、恢复演练；删除在备份中的处理（立即擦除或到期不可恢复）须在真实数据进入前批准并告知。

## 9. 安全发布门槛

- [ ] 家庭/角色/设备权限有正向和反向测试，无 ID 枚举或跨家庭访问。
- [ ] 外部输入、文件、URL（若有）、离线事件、AI 输出和错误路径已测试。
- [ ] 无明文密钥、真实儿童数据或 Restricted 内容泄漏；日志字段通过 allowlist/脱敏测试。
- [ ] 依赖、容器和构建链扫描无未批准 Critical/High 问题。
- [ ] 模型/Prompt/Policy/Schema 版本可追溯；低置信度、直接代答和敏感内容测试通过。
- [ ] 图片保留、家庭导出/删除、缓存和备份处理完成演练。
- [ ] 成本/滥用、授权异常、备份/删除失败告警已演练。
- [ ] 适用法域、监护人同意、保留期限、数据处理方和安全联系渠道已批准。

## 10. 漏洞与事件响应

- 报告渠道：`TBD（项目 Owner 在首次公开/试用前建立安全邮箱或私密渠道）`。
- 分级：Critical（跨家庭/密钥/大量 Restricted 数据/远程控制）、High（单家庭敏感泄露、权限提升、记录完整性破坏）、Medium、Low；最终模型和 SLA 由安全 Owner 批准。
- 隔离/缓解：撤销令牌/密钥、关闭功能开关或 Provider、隔离上传/队列、阻断发布、保留证据；任何删除证据或通知外部人员需授权。
- 通知：适用法律、用户通知和第三方联络由安全/法务 Owner 决定；不得自行对外披露儿童数据事件。
- 复盘：记录时间线、影响范围、根因、检测/响应差距、修复、数据处置和防复发；高风险问题需回归测试和 ADR/Runbook 更新。

## 11. 未决安全事项

| 项目 | Owner | 截止条件 | 影响 |
| --- | --- | --- | --- |
| 适用法域、监护人同意和隐私条款 | 安全/法务 Owner | 任何真实儿童数据进入前 | production 阻塞 |
| 身份提供方、令牌 TTL、MFA/恢复 | 技术 + 安全 Owner | ADR-0005 批准、P0 身份实现前 | 认证架构阻塞 |
| 真实数据法域、备份与正式告知 | 安全/产品 Owner | 任何真实儿童图片/生产前 | 删除和告知阻塞；默认图片保留值见 ADR-0011 |
| Secret Manager、加密和环境拓扑 | 运维 + 安全 Owner | ADR-0008 批准、staging 前 | 部署阻塞 |
| AI Provider 数据处理条款、区域和训练退出 | 产品 + 安全 Owner | ADR-0004/0006 批准、接入真实 Provider 前 | AI 数据出境/再利用风险 |
| 安全扫描、SBOM、签名和报告渠道 | 技术负责人 | 首次公开/发布前 | 供应链与响应能力 |
